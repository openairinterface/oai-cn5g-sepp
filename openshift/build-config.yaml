# SPDX-License-Identifier: MIT
# ---------------------------------------------------------------------
#
kind: BuildConfig
apiVersion: build.openshift.io/v1
metadata:
  name: "sepp-build-cfg"
spec:
    runPolicy: "Serial"
    source:
        type: "Binary"
        secrets:
          - secret:
              name: etc-pki-entitlement
            destinationDir: etc-pki-entitlement
    strategy:
        dockerStrategy:
            dockerfilePath: "docker/Dockerfile.sepp.rhel9"
    output:
        to:
            kind: "ImageStreamTag"
            name: "oai-sepp:latest"
